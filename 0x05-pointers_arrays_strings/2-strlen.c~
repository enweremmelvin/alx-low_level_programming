/**
 * _strlen: find the length of provided string
 * @s: string to be  parsed
 * Return: return the string length
 */
int _strlen(char *s)
{
	int *ptr;
	int length;

	ptr = &s;
	length = strlen(*ptr);

	return (length);
}
